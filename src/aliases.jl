const Tup = Tuple
const Varg = Vararg

const NTup = NTuple
const TupN{T,N} = NTup{N,T}

const AArr = AbstractArray
const ArrA{N,T} = AArr{T,N}

const AVec = AbstractVector
const AMat = AbstractMatrix

const AURange = AbstractUnitRange

const CartInd = CartesianIndex
const CartInds = CartesianIndices
